# Global Brewfile. rcm symlinks this to ~/.Brewfile, so `brew bundle --global`
# reinstalls the toolchain on a fresh Mac.

tap "thoughtbot/formulae"

# dotfiles + shell
brew "rcm"
brew "starship"   # prompt (config: ~/.config/starship.toml)
brew "eza"        # ls replacement
brew "bat"        # cat replacement
brew "fzf"
brew "tmux"

# dev
brew "git"
brew "asdf"
brew "ripgrep"
brew "the_silver_searcher"
brew "vim"
brew "universal-ctags"
